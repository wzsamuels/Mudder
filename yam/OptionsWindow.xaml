<Window x:Class="Yam.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Yam"
        mc:Ignorable="d"
        Title="Options" Height="389.7" Width="672.3">

    <Grid Margin="20,20,20,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox Grid.Column="0" Grid.Row="0" Height="300" Width="150" Margin="0,0,10,10" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <ListBoxItem Content="Test"/>
        </ListBox>

        <Grid Grid.Column ="1" Grid.Row="0">

        </Grid>

        <Button Grid.Column="1" Name="OkButton" Content="OK" Height="20"
                Width="75" Click="OkButton_Click" IsDefault="True" Grid.Row="1" Margin="312.6,0,-312,0"/>
        <Button Grid.Column="1" Name="CancelButton" Content="Cancel" HorizontalAlignment="Right"  VerticalAlignment="Top" Height="20"
                Width="75" Click="CancelButton_Click" IsCancel="True" Grid.Row="1" Margin="0,0.6,-392.2,-0.2"/>
    </Grid>
</Window>
